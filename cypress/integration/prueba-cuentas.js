describe('Cuentas', function() {
    var a = 1 + 1;
    var b = 2;
    it('Comprobar sumatoria', function() {
        expect(a==b).to.equal(true);
    });
    it('Comprobar resta', function() {
        expect(a-b).to.equal(0);
    });
})